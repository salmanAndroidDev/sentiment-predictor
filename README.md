# sentiment-predictor
a simple neural network that predicts the rating of each sentiment for movie review (positive or negative).

## info
_ The accuracy is 85.7% on 25,000 sentences and 70,000 words training. and 3 interations.
_ it is 3-layers neural network, with embeding layer which makes the network run faster.
